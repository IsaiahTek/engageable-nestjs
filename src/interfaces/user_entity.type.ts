export enum EngagementType {
  repost = 'repost',
  bookmark = 'bookmark',
  share = 'share',
  follow = 'follow',
}

export enum EngagementActionType {
  repost = EngagementType.repost,
  bookmark = EngagementType.bookmark,
  share = EngagementType.share,
  follow = EngagementType.follow,
}
